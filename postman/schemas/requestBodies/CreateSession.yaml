description: Session to create
required: true
content:
  "application/json":
    schema:
      type: object
      properties:
        start_at:
          type: string
          format: date-time
        end_at:
          type: string
          format: date-time
      required:
        - start_at
        - end_at