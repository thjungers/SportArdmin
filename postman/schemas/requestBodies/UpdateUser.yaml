description: Fields of the user to update
required: true
content:
  "application/json":
    schema:
      type: object
      properties:
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string
          format: email